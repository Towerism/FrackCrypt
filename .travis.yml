language: cpp
addons:
  apt:
    sources:
      - george-edison55-precise-backports # CMake 3.2
      - ubuntu-toolchain-r-test
    packages:
      - cmake-data
      - cmake
      - gcc-5
      - g++-5

env:
  - TEST_SUITE=unit
  - TEST_SUITE=integration

script: ci/script.sh

notifications:
  email: false
